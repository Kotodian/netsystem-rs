//! UDP transport operations are owned by the session/transport layer.
//!
//! The app crate exposes session FIFO/message-queue handles only; UDP-specific
//! socket facades stay in the session/transport layer.
