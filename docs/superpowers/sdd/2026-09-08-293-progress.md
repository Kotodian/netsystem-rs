# Issue #293 implementation progress

## Execution contract

Start at task 3. Finish and commit one task before implementing the next.
Only Rust unit tests are added; no C probes, source-reading assertions, or new
integration tests. Do not execute tests during implementation. Final review and
testing follow all implementation tasks. The user authorized visibility decisions
and directed alignment with VPP without extra ownership registration.

## Task 3 commit candidate

- Buffer layout has the 64-byte template, second half, optional trajectory,
  aligned headroom marker, inline pre-data, and trailing data marker. Constant
  assertions verify offsets, sizes and alignment.
- Single-segment operations use signed current_data relative to data[0]. Flag
  positions and advance/reset/put/push/make_headroom/pull follow vendored
  `vlib/buffer.h`, with the explicitly specified Rust slice bounds from ADR-0007.
- Runtime buffer and buffer_mut return direct references, tied to shared and
  exclusive runtime borrows. Runtime get_buffer/get_buffer_mut Result/guard APIs
  are removed. Address conversion stays in core; the hidden cross-crate unsafe
  entries carry graph ownership obligations and reject mutable shared tails.
- DataPlaneMain is no longer Clone. Node and packet-producing transport calls
  borrow it mutably. Worker lookup accepts the existing thread index, following
  session_main_get_worker and tcp_get_worker. Packet writes finish before
  another runtime access. Existing consumers are migrated without adding
  protocol behavior or new ownership tracking.
- The existing inline Rust single-segment test covers the upstream unittest's
  zero-length cases, signed advance, negative pre-data, reset, append/prepend,
  and the ADR-defined headroom/pull slices. No tests were executed.
- The worktree already contained uncommitted BufferMain/raw Index prerequisites
  on resumption. They are retained as required by these Buffer operations.

## Compilation performed

- cargo check --workspace --lib --offline --message-format=short: passed.
- cargo check --workspace --bins --examples --offline --message-format=short:
  passed; no binary or example was executed.
- HAMMER_BUFFER_TRACE_TRAJECTORY=1 cargo check -p hammer-core --lib --offline
  --message-format=short: passed. The default check compiled trajectory disabled.
- rustfmt and git diff --check: formatting only, not behavioral testing.

## Remaining numbered tasks

The live issue was re-read on 2026-09-08 and contains 12 tasks. Task 4 is typed
opaque overlays. Tasks 5–11 are allocation/chain/free, Frame ABI, Next/Pending,
refork, domain release, final shutdown, and complete legacy-model removal.
Task 12 is final review and verification, subject to the user's unit-test-only
and test-timing instructions. These tasks have not been declared complete.

Core legacy allocation/chain/Frame surfaces remain for their numbered migration
tasks. No extra Worker/reference ownership registry was added. No final VPP
review, test execution, benchmark, PR creation or push has been performed.


## Task 4 commit candidate

- Declaration macros enforce C layout, 40/56-byte bounds, alignment, Copy,
  all-bit-pattern validity, no interior mutability, and no uninitialized padding.
  zerocopy derives check nested fields; union expansion checks every member's
  representation and full union extent before generating safe member borrows.
- Packet access uses the typed macro. Raw primary/secondary types and the four
  former Buffer getters are no longer public. Core contains the pointer casts;
  generated owner implementations select a typed region at compile time.
- Service owns primary network state; IP lookup and ICMP request coexist in one
  secondary struct; TCP route/egress share an explicit union; UDP owns egress.
  All packet-owner raw opaque casts and transmutes have been migrated.
- Explicit padding preserves initialized bytes across typed assignment and
  metadata copy. Pool template restoration and secondary retention are unchanged.
- Inline Rust coverage: copy/recycle/address identity in the existing core
  Buffer test; network union access; TCP segment producer to output; UDP
  endpoint producer to output; independent ICMP request retention through IP
  local processing. Existing allocation test was migrated to typed access.
- The current-node closure now receives the mutable runtime borrow required by
  the task-3 direct Buffer access migration; its existing callers are migrated.
- VPP evidence: vlib/buffer.h opaque[10]/opaque2[14]; vnet/buffer.h network
  unions; vlib/buffer_funcs.h metadata-copy and pool-template paths;
  vnet/tcp/tcp_output.c tcp_output_push_ip; vnet/udp/udp_output.c
  udp_output_handle_packet. Coverage is derived from those implementations,
  not claimed as an existing upstream standalone opaque test suite.
- Offline production workspace library compilation, focused test-code
  compilation, and `cargo check --workspace --all-targets --offline` passed. No tests were executed. Final review is deferred until
  all numbered implementation tasks are complete, per user instruction.

## Task 5 commit candidate

Task 4 was committed as `b5038b68`.

- Added the ADR allocation, chain append/link/clone, and five free methods on
  DataPlaneMain. The ADR's concrete allocation signature uses initialized u32
  slices; allocation overwrites only its accepted prefix, including wrapped
  rings, and returns short counts without per-Buffer Result on the new path.
- Pool batch allocation borrows the Worker cache and central state once per
  batch. Core owns one free implementation; trace release occurs after Pool
  critical sections. Every segment selects its own Pool.
- Refcount is AtomicU8 at the existing byte offset. Clone retains every tail
  segment atomically; only the final release restores the first-cacheline
  template. Secondary storage and Physmem mappings remain retained.
- Chain extension publishes head/last immediately and retains partial progress.
  add_data uses the Worker's default Pool and invalidates the cached total;
  append-with-alloc selects the first segment's Pool. Typed chain append keeps
  existing length-valid flags rather than manufacturing cache validity.
- Inline Rust cases cover short allocation/untouched suffix, partial head
  publication, cross-Pool chains, shared-tail lifetime, clone invariants, ring
  wrapping, no-next release, and full-head extension. Sources are
  vlib/buffer.c, vlib/buffer_funcs.h, and the existing full-head scenario in
  plugins/unittest/vlib_test.c; no C tests or source assertions were added.
- Legacy allocation and Frame consumers remain for the explicitly numbered
  task-11 migration/removal; their clone/free implementations already delegate
  to the new core operations. No new compatibility types were introduced.
- `cargo check --workspace --all-targets --offline`: passed, including the
  final inline invariant cases. Formatting and diff checks passed.
  No tests executed; no final feature review performed yet.

## Task 6 commit candidate

Task 5 was committed as `71d82557`.

- Fixed typed Frame now replaces the growable BufferFrame throughout callbacks.
  FramePool groups by rounded allocation size, reinstalls destination offsets,
  and recycles only Frame storage. Debug allocation poisons fresh and recycled
  storage, matching vlib/main.c::vlib_frame_alloc_to_node.
- Node::process is a static mutable-runtime/mutable-node-runtime/mutable-Frame
  function returning a vector count. Obsolete instance function selection and
  TCP registration-only callback fields were removed. Session Queue returns
  its produced vector count rather than its initially empty input length.
- graph_node generates an artifact-local ordinary callback trampoline;
  node_function generates typed scalar/SIMD trampolines and layout metadata.
  Both validate Frame layout and abort on panic inside the owning artifact.
  Runtime installs the ordinary trampoline before an optional SIMD override.
- Existing Frame allocation calls now use destination registration sizes.
  Buffer-only legacy cleanup fixtures retain the default layout until their
  explicit ownership migration in tasks 9 and 11.
- Invocation takes the per-node runtime from its slot and restores the mutated
  value after the call. Inline Rust coverage invokes registered typed Frames
  through real scheduling twice and checks retained state and Frame recycling.
  Its evidence is vlib/main.c allocation/dispatch; it is not an upstream C test.
- Offline workspace all-target compilation passed. No tests executed and no
  final review started. Next/Pending owner replacement, explicit domain Buffer
  release, refork, shutdown, and final legacy removal remain tasks 7–11.

## Task 7 commit candidate

Task 6 was committed as `fc4d0f7f`.

- NodeMain directly owns its node state, Frame size classes, indexed Next
  Frames, destination enqueue owners, and growable Pending vector. Removed
  NodeMain cloning; graph consumers borrow the owning graph directly.
- Pending starts with capacity 32 and dispatch walks its growing length by
  index. No element borrow survives a callback. Empty-node scheduling carries
  node identities separately; empty Frames never enter Pending.
- get/put Next Frame expose initialized typed writable suffixes and unused
  suffix counts. Core checks the full stored extent before borrowing vector
  and auxiliary regions. No checkout owner or unsafe caller API is involved.
- Enqueue-owner changes swap indexed Next states and repair the associated
  Pending link. Partial frames remain appendable after put. Full frames retain
  their Pending allocation while a new Next allocation accepts the suffix.
  Dispatch restores the current reusable Frame or recycles superseded storage.
- Fanout writes selected indices directly into Next suffixes and never removes
  input vectors. Direct destination allocation/enqueue follows VPP's existing
  get_frame_to_node/put_frame_to_node path for externally injected Frames.
- Added inline Rust coverage derived from vlib/main.c's get/put, owner change,
  and pending dispatch: empty put, partial append, owner swap/link repair,
  overflow/recycle, unchanged input vectors, and callback-driven Pending growth
  beyond 32 entries. Test code compiled; no test was executed.
- Offline workspace all-target compilation and formatting/diff checks passed.
  Task 8 still owns exact refork cleanup/replacement and its six ADR cases;
  domain Buffer release, final shutdown, and residual core legacy APIs remain
  explicitly assigned to tasks 9–11. Final review has not started.
